# 操作說明

語言：[繁體中文](操作說明.md)｜[English](docs/operation-en.md)
回到：[README](README.zh-TW.md)｜[驗證報告](docs/validation.md)

> 本儲存庫只含前端原始碼。以下描述的是完整工具的操作流程；求解與診斷後端未公開，
> 單獨執行前端無法完成校正。

---

## 需要先準備的東西

| 項目 | 用途 | 必要性 |
|---|---|---|
| Python 3.10 或更新版 | 後端 | 必要 |
| Node.js 18 或更新版 | 建置網頁介面（只有第一次） | 必要 |
| `numpy`、`scipy` | 數值運算與最佳化 | 必要 |
| `scikit-rf` | Touchstone 讀寫、參考阻抗歸一化 | 必要 |
| `scikit-learn` | 代理模型與 CoP | 必要 |
| `fastapi`、`uvicorn`、`python-multipart` | 後端服務與檔案上傳 | 必要 |
| `pyedb` | 從版面讀取截面（ODB++／EDB） | 選用 |
| Ansys Electronics Desktop | 提供 EDB 原生程式庫；Q2D 求解另需授權 | 選用 |

啟動後瀏覽器會開在 `http://127.0.0.1:8100`。預設埠被佔用時會自動改用 8101 或 8102。

---

## 比較模式

兩個檔案就能用。

1. 分別載入模擬與量測的響應。支援 Touchstone（`.s2p`／`.s4p`…）與 VNA 匯出的 CSV。
2. 按「比較」。

工具會做前置檢查（埠數、埠序、參考阻抗、頻段重疊、被動性、互易性、低頻插入損耗
偏移），把兩份響應對齊到共同頻率網格，畫出幅度與群延遲的疊圖，並給出 RMS 差異。

> **CSV 的頻率欄必須標示單位**（`Frequency(Hz)`、`Freq (GHz)`）。本工具不猜單位——
> 猜錯會讓整份結果差一個數量級，而且不會報錯。

---

## 校正模式

| 步驟 | 動作 | 注意 |
|---|---|---|
| 1 | 載入量測 | 對象要是一段**均勻傳輸線**（校正試片）。拿含過孔與轉角的複雜通道反推材料是病態反問題 |
| 2 | 開啟 Delta-L，載入第二片試片 | 強烈建議。見下節 |
| 3 | 填入截面 | 疊構加線寬即可。**stripline 與 microstrip 選錯會讓 Dk 系統性偏高** |
| 4 | 定義校正區間 | 可以有很多段、不必連續，例如低頻對損耗、高頻抓共振 |
| 5 | 選校正參數與搜尋範圍 | |
| 6 | 開始校正 | |

流程是：DOE 取樣求解 → 擬合代理模型 → 在響應面上最佳化 → 自適應精修 → 真求解器
局部收尾 → 真求解驗證 → 可辨識性分析。

---

## Delta-L：把治具消掉

手上有一對**同截面、不同長度**的試片（例如 1 吋與 3 吋）時一定要用。

勾選「使用 Delta-L」，載入第二片試片。工具會自動判斷哪一份是長線（**由群延遲決定，
不由檔名決定**），從檔名猜出長度讓你確認，然後相減。

相減之後**只有 S21 有意義**，校正區間填 S11 會直接被擋。截面的線長也會自動鎖成 ΔL。

三件事工具會替你把關：

| 把關項目 | 判準 |
|---|---|
| 長度單位錯誤 | 用群延遲反推有效介電常數，落在 1～20 之外就擋（把 mm 填成 inch 會變成 0.15） |
| 治具不一致 | 損耗差外推到零頻若還有殘留，代表兩片試片的治具不同 |
| 頻率取樣太粗 | 相鄰頻點的相位變化超過 π 時無法唯一還原，連群延遲都算不對 |

---

## 從版面讀取截面

線寬、線長、銅厚、疊構、Dk、Df 這些版面裡全都有，不必人工抄。**抄錯不會有徵兆**——
曲線照樣疊得上去，只是答案錯了。最容易抄錯的是帶線的板厚 b：公式要的是兩個參考層
之間的距離，但去查疊構表會看到單層介質厚度，兩者差好幾倍。

操作：在校正模式填入 `.aedb` 目錄路徑，按「讀取網路」。工具會列出所有有走線的網路，
並把像校正試片的（單層、有長度、過孔不超過兩個）排在前面。點一條，讀出它的截面，
按「套用到截面」。

> ODB++ 請先在 Ansys Electronics Desktop 裡匯入成 `.aedb`。轉檔的選項（單位、層對應）
> 會影響後續每一個數字，那些選擇應該由人做。

Dk 與 Df 讀出來的是**起始值**，不是答案。粗糙度不會帶進來：EDB 存的是 Hall-Huray 的
節點半徑，本工具的解析模型用 Hammerstad 的 RMS 粗糙度，兩者是不同的模型，數值不能
互換。

---

## 裁切（選用）

裁出試片周圍的區域，寫成一個新的 `.aedb`。實測在一塊 308 條網路、2667 個圖元的真實
板子上，裁切後剩 4 條網路、50 個圖元（少 98.1%），約 10 秒。

**裁切不進校正流程。** 校正走的是解析截面求解器，只需要截面那幾個數字。裁切的產物是
給你拿去跑 SIwave 或 HFSS 用的。

輸出目錄已存在時工具會拒絕，不會靜靜蓋掉。

---

## 讀懂結果

報告最上面永遠先講**可不可信**，再講數字。結論有三態：

| 結論 | 意思 |
|---|---|
| **可信** | 殘差沒有系統性誤差，每個參數都定得出來 |
| **部分可用** | 沒有東西是錯的，但這組量測撐不起所有參數——報告會列出哪些可以採用 |
| **不可信** | 殘差裡有系統性的未解釋成分 |

判定與對應動作：

| 判定 | 意思 | 該怎麼辦 |
|---|---|---|
| 殘差含固定偏移 | 治具或接頭未去嵌，Df 已被汙染 | 先去嵌，或改用 Delta-L |
| 殘差不是平滑形狀 | 共振或阻抗不連續，不是材料問題 | 檢查幾何模型（過孔、換層、接頭） |
| 某參數定不出來 | 這組量測撐不起這麼多自由度 | 加寬頻段，或固定其中一個參數（**Delta-L 對這件事無效**） |
| 某參數頂到搜尋範圍的邊界 | 範圍可能給得太窄 | 一般情況放寬範圍重跑。**但殘差診斷同時指出治具時不要放寬**——最佳化正在拿那個參數安置治具那段與頻率無關的損耗，範圍給多大就吃多少（實測粗糙度放寬後誤差從 +118% 變 +355%）。先去嵌或改用 Delta-L |
| 兩個模型不同意 | 至少有一個模型不適用於這個結構 | 確認兩者的疊構與幾何輸入相同，再找是哪一項物理不同 |
| 交叉驗證沒跑完 | 要求了但預算不夠 | 提高交叉驗證的求解預算重跑。判定為「狀態不明」，材料庫不會標成可信 |

**殘差乾淨不等於參數可信。** 實測過一個案例：Df 錯 34% 而殘差只有 0.046 dB，因為兩組
參數在資料上分不出來。可辨識性分析就是為了這件事。

---

## 交付物

| 產物 | 格式 | 用途 |
|---|---|---|
| 校正報告 | HTML | 自足、可離線開啟、可寄給別人 |
| 材料庫 | JSON | 因果色散模型參數，下一個專案直接套用 |
| Dk／Df 頻率表 | CSV | 任何工具都吃得下 |
| DOE 原始資料集 | — | 存的是曲線而不是成本值，改了校正區間或權重可在同一批資料上重算 |

DOE 資料集也可以中斷後續跑——工作目錄一樣就會自動沿用。

材料庫會帶著「可不可信」的標記。**一組被治具汙染的 Df 若沒有標記就流到下一個專案，
這個工具就變成了汙染的傳播途徑。**

---

## 報告長什麼樣

以下都是工具真的產生的輸出，不是示意圖。

**殘差診斷**——判定的依據是**成分分解**，不是殘差曲線的外觀。這一條殘差是下降的，
不是水平的；被認定為治具，是因為拆出來有 1.1 dB 與頻率完全無關：

![殘差診斷與成分分解](docs/images/residual-decomposition.png)

**曲線比對**——9 GHz 以上兩條線疊在一起，9 GHz 以下看得到一道間隙。那道間隙就是
治具的 1 dB：

![校正後的疊圖](docs/images/overlay-after-calibration.png)

**校正參數**——每個參數都帶著可信範圍，定不出來的那個會標出來：

![校正參數與可信範圍](docs/images/parameters-with-range.png)

介面本身的截圖尚未收錄；預計與示範影片同一次錄製產出。
